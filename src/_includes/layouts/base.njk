<!DOCTYPE html>
<html lang="en">
  <head>
        <meta charset="UTF-8">

        <meta name="author" content="{{ meta.author.name }}">
        <meta name="description" content="{{ meta.description }}">

	    <link rel="stylesheet" href="/css/style.css">
	    <title>{{ title }}</title>
  </head>
<body class="font-serif leading-tight">

  	{{ content | safe }}

</body>
</html>
