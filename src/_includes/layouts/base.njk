<!DOCTYPE html>
<html lang="en">
  <head>
        <meta charset="UTF-8">

        <meta name="author" content="{{ meta.author.name }}">
        <meta name="description" content="{{ meta.description }}">

	    <link rel="stylesheet" href="/css/style.css">
	    <title>{{ title }}</title>
  </head>
<body>

	{% include "header.njk" %}
	<div class="container p-2">
	<h1 class="text-4xl pb-1">{{ title }}</h1>
  	{{ content | safe }}
	</div>
	{% include "footer.njk" %}

</body>
</html>
