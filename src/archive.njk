---
layout: layouts/page
title: vault
eleventyNavigation:
  key: vault
  order: 2
pagination:
  data: collections.post
  size: 2
  alias: post
---
<p>You have arrived at the vault continue at your own peril...</p>

{% set allPost = collections.post %}

<p class="mt-7">The vault currently contains <b>{{ allPost.length }}</b> posts:</p>

{% include "postsList.njk" %}




{# Below are the beginnings of a paginated version of the above 'postsLists.njk' please consider a work in progress

<div class="ml-6 my-2">
{%- for post in post %}

<ul >
	<li>
      <span>#{{ loop.index }}</span>
      <a class="classicLink" href="{{ post.url }}"> {{ post.data.title }}</a>
      <time datetime="{{ post.data.date | dateToString }}">{{ post.date | readableDate }}</time>
  </li>
</ul>

{%- endfor %}
</div>

#################################################################################################################}
